[pytest]
testpaths = tests
addopts = --strict-markers

markers =
    release_blocker: offline contract coverage whose failure blocks a release
    live_provider: opt-in check that may contact an external provider or live documentation endpoint
    hyperframes_qa: opt-in HyperFrames CLI/browser validation or rendering
