# PR-310 — Provider fault-injection suite

Status: **VERIFIED**

The Phase 3 fault corpus covers rate-limit/5xx retry, malformed identity,
missing/partial artifacts, retryable structured responses, timeout, circuit
opening, approval blocking, and duplicate requests after executor restart.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase3_provider_faults.py
4 passed
```
