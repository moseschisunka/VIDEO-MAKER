# PR-909 — Backlot QA evidence

- Status: **VERIFIED**
- Scope: Make current QA and approval evidence usable from the Backlot state/API/UI.
- Implementation: `backlot/state.py`, `backlot/server.py`, `backlot/ui/board.js`, and `backlot/ui/board.css`.

## Verification

```text
python -m pytest -q tests/backlot/test_server.py -k "qa_endpoint or projects_shape_and_state or health"
node --check backlot/ui/board.js
```

Result: pass. The read-only QA endpoint and board state expose safe project-relative frame paths, video/audio/transcript/voice findings, latest approval decisions, status, and required action. The approval panel provides approve/request-revision/reject controls with reviewer notes.

## Remaining risk

No live browser session was used for this local gate; the UI remains subject to Phase 11 human review on the supported deployment.
