# PR-705 — Animation/keyframe quality

Status: **VERIFIED (contract level)**

Animated canonical cuts must declare at least three temporal keyframes. The
generated HTML exposes `data-motion-keyframes`, and `index.motion.json`
contains seek samples and `keepsMoving` assertions for HyperFrames inspect.
Static images are explicitly marked static rather than being misreported as
real motion.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase7_gate.py
8 passed
```

Browser-level motion inspection remains held with the runtime blocker in
`PR-703`.
