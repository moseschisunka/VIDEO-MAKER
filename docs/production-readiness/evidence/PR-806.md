# PR-806 — Audio and caption failure injection

Status: **VERIFIED**

Failure tests now prove that the finishing path fails closed for full silence,
missing audio channels, clipped samples/over-limit true peak, caption timing
past the video, unverified or wrong-language transcripts, overlapping cues, and
text drift. The measured audio report includes codec, sample rate, channels,
layout, integrated LUFS, true peak, clipping samples, and silence intervals.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase8_caption_render.py tests/contracts/test_phase8_audio.py
19 passed in 3.11s
```

The tests use generated media fixtures, so they exercise FFmpeg meters and the
same caption contract used by the runtime adapters rather than mocked pass
values.
