# PR-101 — Repair and validate all manifests

**Date:** 2026-09-02
**Task status:** `VERIFIED` — all 13 manifests load through the strict schema
**Owner:** OpenMontage execution agent
**Depends on:** `PR-100` (`VERIFIED`)
**Next task:** `PR-102` — canonical pipeline catalog

## Change

The `documentary-montage` manifest used the semantically correct category
`documentary`, but the schema did not admit that category. The strict manifest
schema now includes `documentary`, preserving the pipeline's domain identity
instead of silently relabelling it as a different workflow.

No other manifest was silently rewritten. The effective metadata loader from
PR‑100 supplies conservative non-production defaults for legacy manifests until
their explicit release metadata is populated and certified in later Phase 1
work.

## Verification

Commands run from the repository root:

```text
python -m pytest tests/contracts/test_phase1_manifest_validation.py tests/contracts/test_phase1_manifest_metadata.py -q
6 passed

python -c "from backlot.server import _load_pipelines_data; print([(p['id'],p['schema_valid'],p['release_lane']) for p in _load_pipelines_data()])"
12 user-visible records; all schema_valid=True; framework-smoke remains hidden
```

The manifest validation contract confirms all 13 files load, the documentary
category is preserved, and every non-test manifest stage has a readable director
skill. No provider or external service was contacted.

## Acceptance decision

`PR-101` is verified. The previous schema blocker is repaired and every manifest
now has a deterministic load result. `documentary-montage` remains held in the
experimental lane and is not creatable; `framework-smoke` remains test-only and
hidden. PR‑102 may now freeze one canonical filtered catalog for the CLI, API,
and UI. Production remains ineligible until every roadmap gate through `PR-11G`
passes.
