# PR-907 — Non-pass output blocks completion

- Status: **VERIFIED**
- Scope: `pass` is the only final-review status allowed to complete compose or publish.
- Implementation: `tools/video/video_compose.py`, `lib/manifest_executor.py`, and `lib/work_order.py`.

## Verification

```text
python -m pytest -q tests/contracts/test_phase9_quality.py tests/contracts/test_phase9_gate.py
```

Result: pass. `revise` and `fail` renders return unsuccessful tool results; compose rejects non-pass reviews before writing canonical artifacts; publish requires a persisted pass review. Missing review is also a hard blocker.

## Remaining risk

Phase 10 must add operational alerting for repeated non-pass outcomes and stuck revisions.
