# PR-708 — Offline, concurrency, and timing tests

Status: **VERIFIED (adapter contract; live runtime pending)**

The Phase 7 gate covers non-zero narration/music offsets, audio stem
parameters, strict unknown-cut rejection, offline no-npm behavior, repeatable
workspace digests, run-path isolation, inspect ordering, and conservative
worker selection. Existing HyperFrames adapter regressions remain green.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase7_gate.py tests/tools/test_hyperframes_compose.py
51 passed in 75.25s
```

Real offline browser renders and two simultaneous CLI projects require the
runtime installation recorded in `PR-703`.
