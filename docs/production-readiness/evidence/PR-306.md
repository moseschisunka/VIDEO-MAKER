# PR-306 — Ranked selector dry-run plans

Status: **VERIFIED**

TTS, image, video, and music selectors expose a `rank` operation that returns a
ranked candidate list, selected candidate, alternatives, estimated cost,
availability, approval requirement, and a `dry_run=true` plan. Ranking never
invokes a provider.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase3_selector_plans.py tests/contracts/test_phase3_provider_migration.py
4 passed
```

The prior catalog expectation was also repaired to include the live Azure,
Edge TTS, and Fish Audio provider identities rather than hiding them.
