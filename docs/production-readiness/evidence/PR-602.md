# PR-602 — Stock download hardening

Status: **VERIFIED**

`download_stream_atomic` and `download_candidate_atomic` stream to `.part`,
check HTTP status and announced length, reject non-media MIME responses,
validate magic signatures and decoded media, then atomically promote. Corpus
and direct-search production paths also validate before promotion.

Evidence: Phase 6 gate download corruption/MIME/length test passes.
