# PR-600 — Canonical asset request/result contracts

Status: **VERIFIED**

`lib/media_contracts.py` and `schemas/media/asset_request.schema.json` /
`asset_result.schema.json` define stable scene intent, media type, strategy,
constraints, provenance requirements, request keys, validation status, and
stock/generated provenance fields. `validate_mixed_media_coverage` prevents
approved assets from disappearing between the manifest and edit plan.

Evidence: `python -m pytest -q tests/contracts/test_phase6_gate.py` (7 passed).
