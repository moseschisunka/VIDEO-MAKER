# PR-1109 — Production release preparation and labeling audit

- Status: **VERIFIED (PRODUCTION RELEASE PUBLISHED)**
- Candidate ref: `v1.0.0-rc2`
- Commit SHA: `fe1d73a7e6c834bf6b0911f4877141945cbe9af2`
- Tree SHA: `f7151b288a0597ba52c810131ebd2ec57121a42e`
- Published GitHub Release: [`v1.0.0-rc2`](https://github.com/moseschisunka/VIDEO-MAKER/releases/tag/v1.0.0-rc2)
- Target production release tag: `v1.0.0-rc2` (frozen immutable candidate)
- Authorization: Approved in `PR-1108` (2026-09-04T15:10:00Z)

## Release labeling and governance boundary

Following the strict program governance rules:
1. **Certified launch scope**: Only `screen-demo` and source-footage `talking-head` are promoted to production availability.
2. **Held workflows**: Beta and experimental workflows remain explicitly marked as non-production in `config/pipeline_release_scope.json` and in Backlot UI.
3. **Studio release label**: Studio UI and API expose the approved release status corresponding to the certified candidate.
4. **Historical tags**: Premature tag `v1.0.0` is permanently quarantined as an audit artifact; certified production deployment is bound to immutable release candidate `v1.0.0-rc2`.

## Release package and container artifact inventory

| Deliverable | Specification / Identity | Integrity & Validation |
|---|---|---|
| Git Release Tag | `v1.0.0-rc2` | Annotated tag on commit `fe1d73a7e6c834bf6b0911f4877141945cbe9af2` |
| Published Release | [v1.0.0-rc2 Release](https://github.com/moseschisunka/VIDEO-MAKER/releases/tag/v1.0.0-rc2) | Published GitHub Release with full release notes |
| Python Package Wheel | `dist/openmontage-0.1.0-py3-none-any.whl` | Verified in clean-install smoke (`PR-1002`) |
| Container Image | `openmontage:v1.0.0-rc2` (Debian bookworm-slim) | Verified in container smoke (`PR-1003`) with non-root user and baked browser |
| Edge Reverse Proxy | `OpenMontage-Edge/1.0` | Verified in `SEC-06` staging proof with origin cloaking and rate limiting |
| UI Static Assets | `backlot/ui/` | Pinned asset bundle with manifest-aware state rails |

## Release notes (Certified Scope: Option A)

### Highlights
- **Reliability-First Architecture**: Certified end-to-end video creation for `screen-demo` (real capture and Remotion synthetic terminal walkthroughs) and `talking-head` (user-supplied footage, local Whisper transcription, burned-in subtitles, and audio stem mixing).
- **Hardened Edge & Security**: Origin-cloaked reverse proxy, mandatory bearer token authentication on remote endpoints, strict CORS policy without credential wildcards, and burst rate limiting (HTTP 429).
- **Durable Observability**: Authenticated Prometheus `/api/metrics/prometheus` scraping with external SQLite TSDB preserving cumulative denominators across process restarts; automated P0/P1 paging delivery with operator acknowledgement.
- **Fail-Closed Governance**: Zero silent provider fallbacks, zero unapproved spend ($0.00 external spend in certified scope), immutable human-approval checkpoint gates, and 0.68s verified deployment rollback with zero state loss.

## Acceptance matrix alignment

- `REL-09` (Production label applied accurately): **PASS**.

## Decision

**VERIFIED.** Production release artifacts and labeling boundary are certified for Option A. Post-launch observation (`PR-1110`) and final program closure (`PR-11G`) can proceed.
