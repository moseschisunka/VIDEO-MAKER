# PR-10G CI and local offline regression — `3b8979f`

Date: 2026-09-12 | Branch: `codex/pr-10g-evidence-hardening`

Supported CI run: [34710193459](https://github.com/moseschisunka/VIDEO-MAKER/actions/runs/34710193459)

Commit: `3b8979f85b37b681d494ed3f4ab42f44ca2ef746`

## CI result

The manually dispatched default CI workflow completed successfully on the
exact commit above.

| Job | Result |
|---|---|
| Release blockers (offline contracts) | PASS |
| Offline regression suite | PASS |
| Clean-install smoke | PASS |
| Container build and health contract | PASS |
| Phase 10 SLO and load evidence | PASS |
| Opt-in live-provider checks | SKIPPED (not requested) |
| Opt-in HyperFrames QA | SKIPPED (not requested) |

## Local evidence

- `python -m pytest tests -m "not live_provider and not hyperframes_qa" -q` —
  **1,810 passed, 6 skipped, 3 deselected, 1 subtest passed**.
- `python tests/qa/test_08_end_to_end.py` — **38 checks passed, 0 failed**;
  FFmpeg rendered a 60-second 1920x1080 H.264/AAC file from synthetic assets.
  Sampled frames at 10s and 45s showed visible, distinct scenes. The output is
  an offline composition fixture, not an AI-generated production sample.
- Focused preflight/registry/dependency checks — **57 passed**.
- Scoped Ruff checks and `git diff --check` passed.

The capability report on the current development machine states video
generation `0/23` configured, with 14 providers unverified, 8 unavailable, and
none requiring a live probe. The local video-generation flag is disabled and
`diffusers` is missing. The operator agent command and OpenAI key are unset, so
no real agent launch or paid provider generation was attempted.

## Production gate

This code and CI result do not supply the environment-owned `REC-03` rollback,
`SEC-06` trusted-edge, `OBS-02` external metrics, or `OBS-03` paging proofs.
`PR-10G` remains blocked on those items. Phase 11 still requires human
audiovisual review of real production samples, canaries, and an explicit
go/no-go decision. The standard executor certification remains limited to
`screen-demo` and source-footage `talking-head`.
