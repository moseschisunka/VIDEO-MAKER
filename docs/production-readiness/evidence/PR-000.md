# PR-000 — Repository and environment baseline

Date: 2026-09-02

Phase: 0 — Baseline and release control

Status: VERIFIED (task evidence captured; Phase 0 gate still open)

Repository: `C:\Users\PC\Desktop\Open Montage\OpenMontage`

## Baseline identity

| Field | Result |
|---|---|
| Branch | `main` |
| HEAD | `086774b94b2ed1c1150ae4e089b4fc998988dceb` |
| Working tree | Dirty before this baseline; pre-existing user work preserved |
| Status count | 69 porcelain entries: 42 modified/staged and 27 untracked |
| Baseline source changes during PR-000 | None |
| Roadmap docs | Already present as task artifacts before this baseline |

The dirty tree includes Backlot, configuration, Remotion, schemas, tools, tests, and new project/template files. The complete status must be rechecked before every implementation task. No reset, checkout, deletion, credential change, provider call, or source edit was performed for PR-000.

## Toolchain and dependency state

| Component | Observed value | Interpretation |
|---|---|---|
| OS | Windows 11 build `10.0.26200` | Reference environment for this baseline |
| System Python | 3.13.2 at `C:\Users\PC\AppData\Local\Programs\Python\Python313\python.exe` | Used for baseline commands |
| Repository `.venv` | POSIX layout (`bin/python`), no `Scripts/python.exe` | Windows activation contract is not currently aligned; route to Phase 10 |
| Node.js | 22.13.1 | Remotion/HyperFrames prerequisite family present |
| npm | 10.9.2 | Present |
| FFmpeg | 7.1 essentials build | Present |
| ffprobe | 8.1 full build | Present, but version differs from FFmpeg; certify as one supported pair in Phase 10 |
| Remotion package | 4.0.484 | Installed in `remotion-composer/node_modules` |
| TypeScript | 5.9.3 | `tsc --noEmit` passes when executed from `remotion-composer` |
| Node dependency tree | `npm ls --depth=0` reports five extraneous WASM packages | Route to dependency/package cleanup (`PR-1000`–`PR-1002`) |
| Python syntax | `python -m compileall -q lib tools backlot` passes | Baseline syntax check passes |

## Pipeline manifest validation

Command family: load each `pipeline_defs/*.yaml` through `lib.pipeline_loader.load_pipeline_readonly`.

| Manifest | Result |
|---|---|
| `animated-explainer` | PASS |
| `animation` | PASS |
| `avatar-spokesperson` | PASS |
| `character-animation` | PASS |
| `cinematic` | PASS |
| `clip-factory` | PASS |
| `documentary-montage` | FAIL — category `documentary` is rejected by `pipeline_manifest.schema.json` |
| `framework-smoke` | PASS — must remain test-only |
| `hybrid` | PASS |
| `localization-dub` | PASS |
| `podcast-repurpose` | PASS |
| `screen-demo` | PASS |
| `talking-head` | PASS |

Result: 12 of 13 manifests load; one confirmed schema/manifest blocker routes to `PR-101`. The test-only manifest must not be presented as a production workflow.

## Capability preflight

The registry was discovered in a fresh process and queried twice in the same process.

| Measurement | Result |
|---|---:|
| Cold discovery + summary | 104.3499 seconds |
| Warm summary | 29.2020 seconds |
| Setup offers | 56 |
| Composition runtimes | FFmpeg `true`, Remotion `true`, HyperFrames `false` |

Configured/total capability counts:

| Capability | Configured/total | Available providers observed |
|---|---:|---|
| analysis | 9/13 | ffmpeg, ffprobe, local, multi, whisperx, youtube-transcript-api |
| audio_processing | 2/2 | ffmpeg |
| avatar | 0/4 | none |
| character_animation | 6/6 | openmontage |
| clip_acquisition | 1/1 | openmontage |
| clip_retrieval | 0/1 | none |
| corpus_population | 0/1 | none |
| enhancement | 2/6 | ffmpeg |
| graphics | 2/3 | mermaid, pygments |
| image_generation | 0/14 | none |
| music_generation | 0/5 | none |
| music_library | 0/1 | none |
| music_search | 1/2 | pixabay_music |
| packaging | 2/2 | openmontage |
| publish | 1/1 | local |
| screen_capture | 2/2 | cap, ffmpeg |
| source_ingest | 0/1 | none |
| subtitle | 2/2 | openmontage, remotion |
| tts | 1/11 | edge_tts |
| video_generation | 0/23 | none |
| video_post | 8/9 | ffmpeg |

Runtime warnings:

1. HyperFrames npm package was not resolvable within its 5-second probe timeout; HyperFrames is not certified or available in this environment.
2. The ComfyUI video resource profile warns that the top-level profile is not a promise that every workflow fits 8GB VRAM; bundled WAN 2.2 14B FP8 workflows recommend 16GB VRAM.

Interpretation: the current machine can run local FFmpeg/Remotion composition and Edge TTS, but it has no configured image-generation or video-generation provider and no available HyperFrames runtime. This is a capability baseline, not permission to silently substitute providers or media types.

## Test baseline

### Contract suite

Command: `python -m pytest tests\\contracts -q`

Result: **754 passed, 1 failed, 7 skipped in 222.80 seconds**.

Failure:

- `tests/contracts/test_phase3_contracts.py::TestCapabilityMetadata::test_registry_catalog_views`
- The test expects an older TTS provider set. The live registry additionally exposes `azure`, `edge_tts`, and `fish_audio`.
- Route to provider-contract/catalog work (`PR-300`, `PR-306`, `PR-309`). Do not hide new providers or weaken the test.

### Full offline suite

Command: `python -m pytest tests -q`

Result: **1,202 passed, 5 failed, 11 skipped, 53 warnings, 1 subtest passed in 394.12 seconds**.

Failures:

1. `tests/contracts/test_phase3_contracts.py::TestCapabilityMetadata::test_registry_catalog_views` — provider catalog expectation drift; route to `PR-300`/`PR-306`/`PR-309`.
2. `tests/tools/test_remotion_diagnostics.py::test_render_failure_surfaces_remotion_stderr_tail` — `NameError: edit_decisions` at `tools/video/video_compose.py:1900`; route to `PR-200`.
3. `tests/tools/test_remotion_diagnostics.py::test_timeout_expired_gives_actionable_message` — same Remotion `NameError`; route to `PR-200`.
4. `tests/tools/test_remotion_diagnostics.py::test_remotion_timeout_ms_is_passed_through` — same Remotion `NameError`; route to `PR-200`.
5. `tests/tools/test_remotion_diagnostics.py::test_no_timeout_flag_when_not_requested` — same Remotion `NameError`; route to `PR-200`.

Warnings include FastAPI `on_event` deprecations and a Starlette/httpx deprecation. They are not silently treated as release blockers, but must be tracked during Phase 10 maintenance.

### Additional checks

| Check | Result |
|---|---|
| Contract test collection | 762 tests collected |
| Python compileall | PASS |
| Remotion TypeScript check from composer directory | PASS |
| HyperFrames doctor/live runtime | NOT RUN as a successful check; registry probe reports package resolution timeout |

## PR-000 conclusion

PR-000 is verified because the repository, environment, capabilities, manifests, dependency state, timing, and tests are recorded without modifying product behavior.

The program remains **not production-ready**. The next dependency-ready task is `PR-001`, which must convert these observations and the prior audit findings into the owned traceability register. The next remediation task after Phase 0 is not selected until `PR-0G` passes.

Required next action: update `PROGRESS_TRACKER.md`, then execute `PR-001` only.
