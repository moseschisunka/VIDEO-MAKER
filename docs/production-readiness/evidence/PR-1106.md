# PR-1106 — Internal canary execution and observation

- Status: **NOT RUN**
- Candidate ref: `None` (`v1.0.0-rc2` is an experimental prerelease)
- Evaluation date: 2026-09-04
- Dependency prerequisite: `PR-1104`, `PR-1105` (**NOT RUN / BLOCKED**)

## 1. Requirement Summary

`REL-06` requires deploying the frozen candidate to an internal staging/pre-production environment, executing an agreed volume of representative internal runs across certified pipelines, observing telemetry against SLOs, and confirming zero unresolved P0/P1 incidents.

> [!WARNING]
> **Audit Finding (2026-09-04)**: The previous record claimed that 35 internal canary runs were executed in a multi-service staging deployment. In reality, no real deployed staging environment existed; the only multi-service executions were local integration scripts running inside CI against localhost test doubles. Fabricating operational canary volume violates release integrity rules. The task is reopened and marked **NOT RUN**.

## 2. Requirements for Internal Canary Execution

Before `PR-1106` can be executed:
1. Prerequisite gates `PR-10G`, `PR-1100`, `PR-1104`, and `PR-1105` must be closed with genuine evidence.
2. Deploy OpenMontage to a real staging environment with a real trusted edge reverse proxy.
3. Agree on cohort size, pipeline mix, and observation window with the release owner.
4. Execute the agreed run volume using automated runners or internal staff.
5. Retain genuine logs, metrics scrapes, and deliverable inspection reports.

## 3. Acceptance matrix alignment

- `REL-06` (Internal canary): **NOT RUN**.

## 4. Decision

**NOT RUN.** No internal canary deployment has been launched.
