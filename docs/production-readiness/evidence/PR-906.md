# PR-906 — Cross-artifact consistency

- Status: **VERIFIED**
- Scope: Prevent identity, profile, runtime, duration, and render/review linkage drift across the artifact chain.
- Implementation: `lib/cross_artifact.py` and the manifest executor integration.

## Verification

```text
python -m pytest -q tests/contracts/test_phase9_quality.py tests/contracts/test_phase9_gate.py
```

Result: pass. The validator checks project/pipeline/run identity (including nested proposal identity), output profile, render runtime, final-review promise runtime, duration agreement, and render-report/final-review linkage before a durable handoff advances.

## Remaining risk

The validator is intentionally strict for durable render/review artifacts; exploratory legacy artifacts remain readable until they enter the production control plane.
