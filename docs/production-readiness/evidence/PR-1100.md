# PR-1100 — Release candidate freeze and inventory

- Status: **REOPENED / CANDIDATE AUDIT INTEGRITY INCOMPLETE**
- Candidate ref: `v1.0.0-rc2` (GitHub Prerelease)
- Tagged commit: `fe1d73a7e6c834bf6b0911f4877141945cbe9af2`
- Branch HEAD at the 2026-09-04 audit snapshot: `d8da62d` (not the current checkout; subsequent commits exist)
- Tree SHA at tag: `f7151b288a0597ba52c810131ebd2ec57121a42e`
- Evaluation date: 2026-09-04
- Dependency prerequisite: `PR-10G` (**BLOCKED** on environment-owned operational proofs)

## 1. Candidate identity audit

> [!WARNING]
> **Audit Finding (2026-09-04)**:
> 1. `PR-10G` prerequisite is **BLOCKED**. Under release governance rules, Phase 11 gates cannot proceed until `PR-10G` passes with environment-owned operational evidence.
> 2. Candidate identity is inconsistent: the git tag `v1.0.0-rc2` points to commit `fe1d73a`, but subsequent commits were pushed to `main`. While some later changes are documentation-only and later corrective fixes are independently tested, a frozen production release candidate must correspond to an exact, immutable commit tree across code, documentation, and deployment artifacts.
> 3. Tag `v1.0.0-rc2` is published on GitHub as a **pre-release**, not a production release.

| Field | Value | Verification source |
|---|---|---|
| Git tag | `v1.0.0-rc2` | Published GitHub Pre-release |
| Tagged commit SHA | `fe1d73a7e6c834bf6b0911f4877141945cbe9af2` | `git rev-parse v1.0.0-rc2` |
| Branch HEAD at audit snapshot | `d8da62d` | 2026-09-04 audit snapshot; later commits exist |
| Package version | `0.1.0` (candidate `v1.0.0-rc2`) | `pyproject.toml` |
| Operating system | Ubuntu 24.04 LTS (CI reference runner) | GitHub Actions runner image |
| Python runtime | Python 3.10.x | Supported CI `.venv` Python |
| Node runtime | Node.js 22.x / npm 10.x | Pinned CI toolchain |
| FFmpeg / ffprobe | 6.1.1+ (profile-normalized fast encode) | Ubuntu 24.04 apt package |
| Remotion composer | 4.0.245 (locked, 0 vulnerabilities) | `remotion-composer/package-lock.json` |
| HyperFrames runtime | 0.8.25 + vendored GSAP 3.15.0 | Offline certified cache |
| Container baseline | Debian bookworm-slim, non-root user (`openmontage`) | `Dockerfile`, `PR-1003` smoke |
| Scope target | Option A: `screen-demo` + source-footage `talking-head` | `PR-002-SCOPE-2026-09-02` standing approval |
| Certified provider set | Offline-certified (`local ffmpeg`, local stock/diagram, fake/mock contracts); `edge_tts` is network-dependent and is not offline-certified | `tools/capability_registry.py`, `tools/audio/edge_tts.py` |

## 2. Certified pipeline inventory (Option A Scope)

Following the release owner's decision `PR-002-SCOPE-2026-09-02` (reliability-first option A), the intended launch envelope is restricted to:

1. **`screen-demo`**:
   - Capabilities: Real screen/window capture, synthetic Remotion `TerminalScene`, CLI demonstration.
   - Quality/governance gates: Manifest-aware stage progression, immutable approval checkpoints, deterministic duration calculation, fail-closed media probes.
2. **`talking-head`** (source-footage only):
   - Capabilities: User-supplied video/audio, local transcription, project-relative caption burn-in, audio stem mixing, Remotion/FFmpeg compose.
   - Quality/governance gates: Strict ingestion stream validation, positive finite timeline check, contrast verification, immutable approval gates.

### Held / uncertified pipelines (retained in product as beta/experimental)

- **Beta:** `cinematic`, `clip-factory`, `podcast-repurpose`, `hybrid`, `animation`, `character-animation`.
- **Experimental:** `animated-explainer`, `avatar-spokesperson`, `localization-dub`, `documentary-montage`.
- **Test only:** `framework-smoke`.

## 3. Dependency inventory

- **Authoritative Python dependencies** (`pyproject.toml` / `requirements.txt`):
  - `fastapi>=0.100.0`, `uvicorn>=0.22.0`, `pydantic>=2.0.0`, `pydantic-settings>=2.0.0`
  - `pyyaml>=6.0`, `jinja2>=3.1.2`, `requests>=2.31.0`, `httpx>=0.24.0`
  - `psutil>=5.9.0`, `edge-tts>=6.1.0`, `python-dotenv>=1.0.0`
  - `pip-audit` reports **0 known vulnerabilities**.
- **Locked Remotion dependencies** (`remotion-composer/package-lock.json`):
  - `@remotion/cli@4.0.245`, `@remotion/bundler@4.0.245`, `remotion@4.0.245`
  - `react@18.2.0`, `react-dom@18.2.0`
  - `npm audit --audit-level=high` reports **0 vulnerabilities**.

## 4. Capability inventory status

Discovered by `tools/capability_registry.py`:
- `tts`: `edge_tts` configured; paid cloud TTS providers bounded/optional.
- `media_compose`: `ffmpeg` available; `remotion` available (13 compositions bundled); `hyperframes` offline-certified.
- `transcription`: local Whisper / ffprobe models available.
- `web_boundary`: Localhost mock reverse proxy verified in CI; deployed trusted edge (`SEC-06`) **pending**.
- `observability`: `/api/metrics/prometheus` and SQLite TSDB metrics sink verified locally; external TSDB (`OBS-02`) and external paging (`OBS-03`) **pending**.
- `recovery`: manifest-hashed state backup/restore (`REC-01`), 0.9 migration (`REC-02`), and local directory rollback verified; real deployment rollback (`REC-03`) **pending**.

## 5. Decision

**REOPENED / BLOCKED.** Release candidate `v1.0.0-rc2` is an experimental GitHub prerelease. Candidate freeze for production certification cannot be certified until prerequisite `PR-10G` is unblocked and candidate identity is reconciled against an exact immutable commit tree.
