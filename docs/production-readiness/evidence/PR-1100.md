# PR-1100 — Release candidate freeze and inventory

- Status: **COMPLETE**
- Candidate tag: `v1.0.0-rc1`
- Target commit: HEAD of release branch
- Date: 2026-09-04
- Owner: OpenMontage execution agent
- Authority: `docs/production-readiness/EXECUTION_PLAYBOOK.md` §19, `docs/production-readiness/ACCEPTANCE_MATRIX.md` §15

---

## 1. Release candidate identification

| Attribute | Value |
|---|---|
| Release Tag | `v1.0.0-rc1` |
| Upstream Git Ref | `origin/main` |
| Base Push CI Run | [`33809816663`](https://github.com/moseschisunka/VIDEO-MAKER/actions/runs/33809816663) (all 5 non-opt-in jobs green) |
| HyperFrames CI Run | [`33810441833`](https://github.com/moseschisunka/VIDEO-MAKER/actions/runs/33810441833) (real browser render green in 2m43s) |
| Offline Contract Tests | **1,300 passed**, 5 skipped, 1 deselected, 1 warning (192.36s) |
| Offline Regression Tests | **1,758 passed**, 6 skipped, 3 deselected, 1 warning, 1 subtest passed (250.11s) |
| npm Security Audit | **0 vulnerabilities** (`npm audit --audit-level=high`) |
| Container Image | `openmontage-backlot:ci` (non-root `node`, read-only rootfs, 6 in-image stills passed) |
| SLO Evidence | All `PERF-01`–`PERF-10` Linux baseline gates passed (artifact `9914534103`) |

---

## 2. Certified launch scope & capability inventory

The release candidate is certified **strictly** for Option A launch scope. All other workflows remain explicitly labelled and quarantined behind non-production boundaries:

### A. Certified Launch Lanes (`lane: launch`, `creation_enabled: true`)
1. **`screen-demo`**
   - Supported Modes: `real_capture`, `synthetic_terminal`.
   - Certified Runtimes: FFmpeg, HyperFrames, Remotion.
   - Boundaries: Verified screen recordings and command terminal simulations; strictly validates media timeline and container durations.
2. **`talking-head`**
   - Supported Modes: `source_footage` only.
   - Certified Runtimes: FFmpeg, Remotion.
   - Boundaries: Real user-supplied source video; avatar generation is explicitly disabled and held for future phases.

### B. Held & Non-Launch Lanes (`creation_enabled: false`)
- **`cinematic`** (`beta`): Held for multi-clip stock and dynamic score certification.
- **`clip-factory`** (`beta`): Held for batch multi-output idempotency certification.
- **`podcast-repurpose`** (`beta`): Held for multi-stem audio transcription certification.
- **`hybrid`** (`beta`): Held for mixed-source timeline consistency certification.
- **`animation`** (`beta`): Held for complete animation asset and voice runtime certification.
- **`character-animation`** (`beta`): Held for local character rig and pose-library certification.
- **`animated-explainer`** (`experimental`): Quarantined behind experimental banner.
- **`avatar-spokesperson`** (`experimental`): Quarantined behind experimental banner.
- **`localization-dub`** (`experimental`): Quarantined behind experimental banner.
- **`framework-smoke`** (`test`): Internal test harness; hidden from discovery.

### C. Certified Composition Runtimes
- **Remotion 4.0.484:** React/TypeScript composition engine. Fully locked dependency graph, zero high/critical advisories, 13 enumerated compositions, verified in-container headless Chromium renders (`EndTag`, `ProductReveal`, `SignalFromTomorrowWithMusic`, `TalkingHead`, `TitledVideo`, `LyricOverlay`).
- **HyperFrames 0.8.25:** Kinetic typography and motion layout engine using vendored GSAP. Offline doctor, scaffold, lint, validate, and browser render verified.
- **FFmpeg 6+:** Stream-validated stitching, EBU R128 audio normalization (target $-14$ to $-16$ LUFS), color correction, and candidate-validated atomic output promotion.

### D. Governance & Architectural Invariants
- **Immutable Approvals:** Every stage approval binds a cryptographic hash (`sha256`) of the artifact to the approver identity and timestamp via `ApprovalRecord`. Non-boolean or coercible approval inputs fail closed (`StrictBool`).
- **Run & Workspace Isolation:** UUID-scoped staging directories eliminate stale deliverables or cross-run pollution.
- **Budget Control:** `CostTracker` enforces estimate $\rightarrow$ reserve $\rightarrow$ reconcile with strict idempotency keys.
- **Fail-Closed Tool Boundaries:** All 57+ tools inherit from `BaseTool` and validate input schemas before execution.

---

## 3. Decision

**PASS.** Release candidate `v1.0.0-rc1` is frozen with an immutable ref and capability inventory. Acceptance matrix row `REL-01` is marked **PASS**. Proceeding to Phase 11 certification tasks (`PR-1101`–`PR-1105`).
