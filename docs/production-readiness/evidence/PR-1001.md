# PR-1001 — Non-Python package data

- Status: **VERIFIED**
- Gate date: 2026-09-02
- Gate owner: OpenMontage execution agent
- Scope: Installed availability of pipeline manifests, Layer 2 skills, styles, schemas, templates, creator profiles, Backlot UI, configuration, ComfyUI workflows, vendored GSAP, and the Remotion composer project.

## Implementation

- Added explicit package markers for the data-bearing top-level directories (`config`, `content_templates`, `pipeline_defs`, `profiles`, `skills`, and `styles`).
- Added `setuptools` package-data rules for JSON/YAML schemas, Markdown/YAML skills, playbooks, templates, creator profiles, Backlot UI assets, ComfyUI workflow JSON, and vendored render assets.
- Added wheel data-file mappings for the legacy root-level `config.yaml` and the complete root-relative `remotion-composer/` tree (source hierarchy, public fixtures, media fixtures, `package.json`, and `package-lock.json`). This preserves the existing runtime path contract while the resource-root migration remains a separate hardening concern.
- Kept generated `node_modules`, project outputs, credentials, and user media out of the package.

## Verification

| Command | Result |
|---|---|
| `python -m pytest tests/contracts/test_phase10_dependencies.py tests/contracts/test_phase10_package_data.py -q` | **6 passed** |
| `python -m pip wheel . --no-deps --no-build-isolation --wheel-dir tmp/pr1001-wheel` | **openmontage-0.1.0-py3-none-any.whl built** (2,059,785 bytes) |
| Wheel archive inspection for required manifest/skill/style/schema/template/UI/Remotion paths | **All required entries present** |
| `python -m pip install --no-deps --target tmp/pr1001-install <wheel>` | **Successful** |
| Installed-target file checks for `config.yaml`, `pipeline_defs`, `skills`, `styles`, `content_templates`, `profiles`, `schemas`, `backlot/ui`, and `remotion-composer` | **All required files present** |
| Installed-target `lib.paths.REPO_ROOT` + `list_pipeline_catalog(...)` from outside the checkout | **13 manifests discovered; config and Remotion paths resolved from the installed root** |

The disposable wheel/install directories were removed after verification. No generated environment, build output, credentials, project data, or user media is part of the release tree.

## Decision

**PASS.** A built and installed OpenMontage distribution contains the non-Python assets required for manifest-faithful execution, Backlot serving, validation, and the Remotion render project. `PR-1002` is the next task; production remains ineligible until `PR-11G`.
