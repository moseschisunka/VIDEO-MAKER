# PR-1102 — Clean-environment certification

- Status: **VERIFIED**
- Candidate ref: `v1.0.0-rc2`
- Tagged commit: `fe1d73a7e6c834bf6b0911f4877141945cbe9af2`
- Tree SHA: `f7151b288a0597ba52c810131ebd2ec57121a42e`
- Verified workflow: [`33871877480`](https://github.com/moseschisunka/VIDEO-MAKER/actions/runs/33871877480)
- Execution date: 2026-09-04

## Clean-environment test results

| Environment / Job | Job ID | Scope | Result | Execution time |
|---|---|---|---|---|
| Clean-install smoke (`PR-1002`) | `101019522304` | Disposable Ubuntu 24.04 runner, fresh `.venv`, dependency installation, wheel validation, CLI smoke | **PASS** | 1m14s |
| Container build & health (`PR-1003`) | `101019858855` | Docker build, non-root user verification (`openmontage`), read-only root FS, healthcheck probe, in-image Remotion Chromium still render | **PASS** | 2m18s |

## Key verification checks passed

1. **Dependency and package verification**:
   - Python dependencies install cleanly without conflicts (`pip check` passes).
   - Package data assets (schemas, pipelines, templates, Backlot UI static files) are present and correctly discovered in the clean package build.
   - `pip-audit` confirms 0 known vulnerabilities.
2. **Container isolation and security**:
   - Runs as non-root user `openmontage` (UID 10001).
   - Container healthcheck (`CMD curl -f http://localhost:8000/api/health || exit 1`) succeeds.
   - Read-only root filesystem with explicit ephemeral scratch mount at `/tmp`.
   - Loopback / non-loopback authentication policy enforced fail-closed.
3. **In-container Remotion rendering**:
   - Headless Chromium and Debian shared library dependencies (`libnspr4`, `libnss3`, etc.) successfully initialize.
   - In-image still render verified without network access, generating non-empty frames stored in retained artifact `openmontage-container-render`.

## Acceptance matrix alignment

- `OPS-01` (Clean Python install): **PASS**.
- `OPS-04` (Package data inspection): **PASS**.
- `OPS-05` (Container build and health contract): **PASS**.

## Decision

**VERIFIED.** The clean-environment certification is complete for frozen candidate `v1.0.0-rc2`.
