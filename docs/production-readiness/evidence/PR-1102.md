# PR-1102 — Clean-environment and container certification

- Status: **COMPLETE**
- Candidate: `v1.0.0-rc1` (`69d510693815b49f0cdefc17aea6b646fb4e63c3`)
- Date: 2026-09-04
- Owner: OpenMontage execution agent
- Authority: `docs/production-readiness/EXECUTION_PLAYBOOK.md` §19, `docs/production-readiness/ACCEPTANCE_MATRIX.md` §14–15

---

## 1. Clean environment verification

The clean setup-to-render procedure was executed in disposable, ephemeral Ubuntu environments in supported CI run [`33811261961`](https://github.com/moseschisunka/VIDEO-MAKER/actions/runs/33811261961):

### A. Python clean environment (`PR-1002`)
- Created a fresh virtual environment `.venv` from system Python 3.11.
- Installed exclusively from authoritative `requirements.txt` and `pyproject.toml`.
- Ran `pip check` — zero broken requirements found.
- Verified package data discovery — 13 manifests and all schemas discovered from clean package wheel.

### B. Remotion clean environment (`PR-1002`)
- Created clean workspace and installed dependencies exclusively via `npm ci` from `remotion-composer/package-lock.json`.
- Ran `npm audit --audit-level=high` — **0 vulnerabilities**.
- Verified pinned browser installation (`npx remotion browser ensure`).
- TypeScript typecheck (`npx tsc --noEmit`) and bundle (`npx remotion bundle src/index.tsx`) passed with zero errors.
- Enumerated all 13 production and demo compositions without missing module errors.

### C. Containerized runtime certification (`PR-1003` / `OPS-05`)
- Built the production container image `openmontage-backlot:ci` from `Dockerfile`.
- Runtime constraints verified:
  - Runs as non-root UID (`node`).
  - Read-only root filesystem (`read_only: true`, `security_opt: no-new-privileges:true`, `cap_drop: ALL`).
  - Ephemeral directories mounted as memory-backed tmpfs (`/tmp`, `/app/.backlot`, `/home/node/.npm`, `/home/node/.cache`).
- Authenticated healthcheck verified: container responds HTTP 200 with `{ "status": "ok" }` when queried with the configured bearer token.
- In-image rendering verified: successfully rendered non-empty still previews for 6 default compositions directly inside the hardened container image (`EndTag`, `ProductReveal`, `SignalFromTomorrowWithMusic`, `TalkingHead`, `TitledVideo`, `LyricOverlay`).

---

## 2. Decision

**PASS.** Release candidate `v1.0.0-rc1` installs, packages, configures, and renders cleanly in isolated disposable environments without developer residue or host machine side-effects.
