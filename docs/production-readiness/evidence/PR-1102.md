# PR-1102 — Clean-environment certification

- Status: **NOT RUN FOR PHASE 11 CREDIT (PACKAGING & CONTAINER BASELINE VERIFIED)**
- Candidate ref: `v1.0.0-rc2` (Prerelease)
- Tagged commit: `fe1d73a7e6c834bf6b0911f4877141945cbe9af2`
- Supported workflows: [`33870762963`](https://github.com/moseschisunka/VIDEO-MAKER/actions/runs/33870762963), [`33871877480`](https://github.com/moseschisunka/VIDEO-MAKER/actions/runs/33871877480)
- Evaluation date: 2026-09-04
- Dependency prerequisite: `PR-1100` (**REOPENED / BLOCKED**)

## 1. Requirement Summary

`PR-1102` requires executing and certifying disposable clean installation and container runtime behavior for the frozen candidate in isolated environments.

> [!WARNING]
> **Audit Finding (2026-09-04)**: Clean install and container smoke jobs pass in supported CI. However, Phase 11 certification credit cannot be awarded because prerequisite `PR-10G` is BLOCKED and candidate freeze `PR-1100` is reopened. The results are retained as baseline packaging and container verification only.

## 2. Clean-environment test results (Supported CI)

| Environment / Job | Scope | Result | Execution time |
|---|---|---|---|
| Clean-install smoke (`PR-1002`) | Disposable Ubuntu 24.04 runner, fresh `.venv`, dependency installation, wheel validation, CLI smoke | **PASS** | 1m14s |
| Container build & health (`PR-1003`) | Docker build, non-root user verification (`openmontage`), read-only root FS, healthcheck probe, in-image Remotion Chromium still render | **PASS** | 2m18s |

## 3. Verified Baseline Checks

1. **Dependency and package verification**:
   - Python dependencies install cleanly without conflicts (`pip check` passes).
   - Package data assets (schemas, pipelines, templates, Backlot UI static files) are present and correctly discovered in the clean package build.
   - `pip-audit` confirms 0 known vulnerabilities.
2. **Container isolation and security**:
   - Runs as non-root user `openmontage` (UID 10001).
   - Container healthcheck succeeds.
   - Read-only root filesystem with explicit ephemeral scratch mount at `/tmp`.
   - Loopback / non-loopback authentication policy enforced fail-closed.
3. **In-container Remotion rendering**:
   - Headless Chromium and Debian shared library dependencies (`libnspr4`, `libnss3`, etc.) successfully initialize.
   - In-image still render verified without network access, generating non-empty frames stored in retained artifact `openmontage-container-render`.

## 4. Acceptance matrix alignment

- `OPS-01` (Clean Python install): Verified in CI baseline.
- `OPS-04` (Package data inspection): Verified in CI baseline.
- `OPS-05` (Container build and health contract): Verified in CI baseline.

## 5. Decision

**NOT RUN / BLOCKED.** Packaging and container mechanics are verified in CI, but formal Phase 11 clean-environment certification remains blocked until prerequisite gates are closed.
