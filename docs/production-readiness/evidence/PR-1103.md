# PR-1103 — Authorized live-provider smoke and boundary audit

- Status: **NOT RUN / EXCLUDED FROM LAUNCH SCOPE**
- Candidate ref: `v1.0.0-rc2` (Prerelease)
- Tagged commit: `fe1d73a7e6c834bf6b0911f4877141945cbe9af2`
- Decision authority: `PR-002-SCOPE-2026-09-02` (Approved: Option A — reliability-first)
- Evaluation date: 2026-09-04

## 1. Provider boundary classification

> [!WARNING]
> **Audit Finding (2026-09-04)**: `PR-1103` previously misclassified offline contract checks using zero-cost local providers as a passing "live-provider smoke test." Offline contract checks with fake/local providers do NOT constitute an authorized live-provider smoke test. Because the approved launch scope (Option A) explicitly excludes all commercial cloud and paid generative providers, no live third-party provider smoke test was authorized or executed. `REL-03` must be recorded as **NOT_RUN / EXCLUDED**, not `PASS`.

## 2. Approved launch provider scope (Option A)

Under `PR-002-SCOPE-2026-09-02`, the certified launch envelope strictly excludes external paid generative cloud APIs:
- **`screen-demo`**: Local desktop/window capture + Remotion synthetic `TerminalScene`. (No external cloud APIs).
- **`talking-head`**: User-supplied input media + local Whisper transcription + local FFmpeg / Remotion compositing. (No external cloud APIs).
- **TTS**: `edge_tts` is zero-key but network-dependent on Microsoft's Edge service; it is not a local/offline provider and is not credited as offline certification. `piper_tts` is the true offline alternative, but it is not currently the selected default.

All commercial paid generative providers (OpenAI, Anthropic, ElevenLabs, Runway, Pika, Kling, Luma) are held in **Beta** or **Experimental** tiers and disabled from the default launch scope.

## 3. Offline provider isolation contracts verified

The following fail-closed provider isolation contracts passed in CI run `33871877480`:
- **Spend isolation**: Fake provider counters verify zero duplicate spend on retry/restart (`INV-04`).
- **Spend approval**: Malformed approval flags fail closed with visible error (`PR-1021`).
- **Circuit breakers**: Offline simulations verify 429/5xx responses trip circuit breaker with backoff (`PR-304`).
- **Secret redaction**: Secret scanner verifies zero API keys or credentials in codebase/image (`SEC-05`).
- **Live external spend incurred**: **$0.00**.

## 4. Acceptance matrix alignment

- `REL-03` (Live provider smoke): **NOT_RUN / EXCLUDED**. Cloud providers are excluded from Option A release scope. No live third-party cloud smoke test was conducted.

## 5. Decision

**NOT RUN / EXCLUDED.** Commercial cloud providers are excluded from the launch scope. No live-provider smoke test was performed.
