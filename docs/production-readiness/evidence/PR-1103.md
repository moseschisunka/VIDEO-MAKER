# PR-1103 — Authorized launch provider smoke matrix

- Status: **COMPLETE**
- Candidate: `v1.0.0-rc1` (`69d510693815b49f0cdefc17aea6b646fb4e63c3`)
- Date: 2026-09-04
- Owner: OpenMontage execution agent
- Authority: `docs/production-readiness/EXECUTION_PLAYBOOK.md` §19, `docs/production-readiness/ACCEPTANCE_MATRIX.md` §15

---

## 1. Launch scope provider inventory

Per the approved Option A release scope (`config/pipeline_release_scope.json`), launch capability is restricted to:
1. `screen-demo` (modes: `real_capture`, `synthetic_terminal`)
2. `talking-head` (mode: `source_footage` only)

The provider stack required for these certified workflows is entirely zero-key and cost-bounded:

| Capability | Certified Launch Provider | Cost Policy | Audit Status |
|---|---|---|---|
| **Voice Synthesis (TTS)** | `edge_tts` (Microsoft Neural en-US-AriaNeural, en-US-GuyNeural) | Free / zero-key | Verified in offline/live unit tests |
| **Voice Synthesis (TTS Offline)** | `piper_tts` (local ONNX neural voice models) | Free / offline | Verified in offline contract tests |
| **Visual Ingestion** | Local source footage / screen captures / terminal capture | Free / local | Stream validation & FFprobe verified |
| **Motion & Compositing** | Remotion 4.0.484 + HyperFrames 0.8.25 | Free / self-hosted | Verified in CI still/browser renders |
| **Audio/Video Muxing** | FFmpeg 6+ (EBU R128 loudness filtering) | Free / local | Verified across all test pipelines |

---

## 2. Paid provider status & quarantine

All third-party cloud generation APIs (OpenAI, Anthropic, Google GenAI, ElevenLabs, Runway, Kling, Suno, Replicate, Fal.ai) are:
- **Excluded from launch scope**;
- Explicitly marked `lane: beta` or `lane: experimental`;
- Quarantined behind disabled creation flags (`creation_enabled: false` in `pipeline_release_scope.json`);
- Protected by `ProviderExecutor` fail-closed budgeting and explicit user approval barriers.

Total live spend incurred for `v1.0.0-rc1` certification: **$0.00 USD**.

---

## 3. Decision

**PASS.** Acceptance matrix gate `REL-03` is marked **PASS**. The launch scope provider stack is certified operational with zero unbudgeted API dependencies.
