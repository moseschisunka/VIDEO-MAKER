# PR-1002 — Clean-install smoke test

- Status: **VERIFIED**
- Gate date: 2026-09-02
- Gate owner: OpenMontage execution agent
- Scope: Disposable Python installation, locked Remotion install hook, manifest validation, registry discovery, Backlot health, and credential-free local rendering.

## Implementation

- Added `tests/contracts/test_phase10_clean_install.py` as a release-blocking smoke gate.
- The gate validates every checked-in pipeline manifest against the manifest schema, discovers the complete tool registry, verifies representative video/TTS/image capabilities, exercises `/api/health` through the real FastAPI app, generates a synthetic local source, composes it through `VideoCompose`/FFmpeg, and verifies the output contains both video and audio streams.
- Added a dedicated `clean-install-smoke` CI job that creates a disposable Python venv, installs `requirements-dev.txt`, installs the exact Remotion graph with `npm ci`, installs the pinned Remotion browser, type-checks and bundles the composer, enumerates compositions, installs FFmpeg, and runs only the PR-1002 gate. No API keys or live providers are used.

## Verification

| Command | Result |
|---|---|
| `python -m pytest tests/contracts/test_phase10_clean_install.py -q` | **1 passed** (local Python 3.13; FFmpeg output verified) |
| `tmp/pr1002-venv/Scripts/python.exe -m pip install -r requirements-dev.txt` | **Successful fresh disposable install** |
| `tmp/pr1002-venv/Scripts/python.exe -m pip check` | **No broken requirements found** |
| `tmp/pr1002-venv/Scripts/python.exe -m pytest tests/contracts/test_phase10_clean_install.py -q` | **1 passed** from the disposable environment |
| Disposable Remotion tree (`npm ci --offline`, TypeScript, bundle, composition enumeration) | **Install/typecheck/bundle/enumeration passed; browser bootstrap requires CI network** — [`PR-10G-remotion-clean-install.json`](PR-10G-remotion-clean-install.json) |
| `.github/workflows/ci.yml` YAML parse | **Valid**; dedicated `clean-install-smoke` job present |

The disposable venv was removed after verification. The CI job is intentionally kept as the repeatable clean-machine proof and installs the locked Node graph before the smoke gate.

## Decision

**PASS.** OpenMontage has a repeatable clean-install smoke gate covering package imports, manifest validation, registry discovery, Backlot health, and a real local render without credentials or unapproved spend. `PR-1003` is the next task; production remains ineligible until `PR-11G`.
