# PR-701 — Narration offsets and scene timing

Status: **VERIFIED (adapter contract)**

Narration segments preserve `start_seconds`, `end_seconds`, and
`offset_seconds`/`data-media-start`. Music preserves its source offset and
the visual root carries explicit duration and FPS. The generated
`audio_plan.json` is stem-preserving and serialisable for runtime inspection.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase7_gate.py
8 passed
```

The test asserts a non-zero narration source offset and a non-zero music
offset survive the canonical mapping.
