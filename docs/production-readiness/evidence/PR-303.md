# PR-303 — Timeout, retry, backoff, and jitter

Status: **VERIFIED**

`ProviderExecutor` retries only classified transient, timeout, rate-limit, or
5xx failures, with bounded exponential delay and injectable jitter/clock
functions. Permanent, malformed, partial-output, approval, and cancellation
classes do not receive an unsafe retry.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase3_provider_executor.py tests/contracts/test_phase3_provider_faults.py
12 passed
```
