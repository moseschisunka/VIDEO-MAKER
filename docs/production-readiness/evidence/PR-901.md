# PR-901 — Gated stages pause correctly

- Status: **VERIFIED**
- Scope: Agent submissions cannot complete manifest-gated stages. They persist `awaiting_human` and stop the work-order pointer.
- Implementation: `lib/checkpoint.py` and `lib/manifest_executor.py`.

## Verification

```text
python -m pytest -q tests/contracts/test_phase9_approval.py tests/contracts/test_phase1_claim_resume.py
```

Result: pass. Completed gated submissions are normalized to `awaiting_human`; the mutable boolean cannot advance a checkpoint; downstream execution does not begin until the approval transition.

## Remaining risk

Existing projects with hand-written legacy checkpoints need migration/observability treatment in Phase 10; new writes are fail-closed.
