# PR-903 — Persist and link final review

- Status: **VERIFIED**
- Scope: A compose handoff must contain a current `final_review` linked to the render report and the exact output bytes.
- Implementation: `schemas/artifacts/final_review.schema.json`, `tools/video/video_compose.py`, and `lib/manifest_executor.py`.

## Verification

```text
python -m pytest -q tests/contracts/test_phase9_quality.py tests/contracts/test_phase9_gate.py
```

Result: pass. Final review carries review ID, review time, output SHA-256, status, checks, and recommended action. Compose rejects missing review, broken `final_review_ref`, mismatched output path, missing output, or digest drift before persistence.

## Remaining risk

The final review is deterministic local evidence; Phase 11 still requires human audiovisual inspection of actual outputs.
