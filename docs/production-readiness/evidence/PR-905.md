# PR-905 — Audio and narration QA

- Status: **VERIFIED**
- Scope: Measure the final muxed stream and compare narration evidence to the authored script and immutable voice/language contract.
- Implementation: `tools/analysis/audio_quality.py`, `tools/video/video_compose.py`, and `lib/voice_contracts.py`.

## Verification

```text
python -m pytest -q tests/contracts/test_phase8_audio.py tests/contracts/test_phase9_quality.py tests/contracts/test_phase9_fault_corpus.py
```

Result: pass. Loudness/true peak/silence/clipping, missing audio, transcript word accuracy, punctuation leaks, language drift, and voice-identity drift route to a corrective action. No provider call is needed for the gate.

## Remaining risk

Perceptual speech intelligibility and pronunciation still require human listening in Phase 11; measured meters are not a substitute for that review.
