# PR-601 — User-media ingestion

Status: **VERIFIED**

`ingest_user_media` requires explicit consent, rejects symbolic links and
destination traversal, copies through a `.part` file, validates magic/decode,
and returns hash, probe, consent, provenance, and ingestion timestamps.

Evidence: Phase 6 gate user-media test passes in
`tests/contracts/test_phase6_gate.py`.
