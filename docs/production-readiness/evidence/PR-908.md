# PR-908 — Seeded QA fault corpus

- Status: **VERIFIED**
- Scope: Deterministic offline defect fixtures prove severity and corrective-action routing.
- Implementation: `tests/eval/qa_fault_corpus.json` and `tests/contracts/test_phase9_fault_corpus.py`.

## Verification

```text
python -m pytest -q tests/contracts/test_phase9_fault_corpus.py
```

Result: **13 passed**. Cases cover missing media, black video, frozen tail, duplicate shots, wrong profile, silent audio, clipped audio, transcript drift, language/voice mismatch, overlay bounds, missing visual source, and placeholder copy. The corpus declares `provider_access: offline_only`.

## Remaining risk

The corpus is intentionally small and deterministic; Phase 10/11 should add bounded real-project soak and authorized provider smoke evidence.
