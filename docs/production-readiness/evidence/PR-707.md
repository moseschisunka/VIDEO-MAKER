# PR-707 — Run-scoped workspace and cleanup

Status: **VERIFIED (path contract)**

When a project/run identity is available, HyperFrames writes to
`runs/<uuid>/work/hyperframes`; production rejects a caller-supplied shared
workspace outside that envelope. Asset copies and generated runtime files use
atomic `.part` promotion and cleanup, and render candidates remain under the
run candidate directory.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase7_gate.py
8 passed
```

The test also verifies that two identical golden inputs produce isolated,
repeatable workspaces.
