# PR-405 — Profile and aspect propagation

Status: **VERIFIED**

Added `lib/format_contracts.py` for pure profile propagation and cross-artifact
validation. Brief, proposal, script, scene, edit, render, and render-output
schemas now accept the canonical profile, alias, aspect ratio, and target
duration fields. Manifest stage submission checks an opted-in profile chain
against the work-order selection, and render reports pass profile facts into
fresh media validation.

Evidence: `tests/contracts/test_phase4_timing.py` profile propagation test;
schema validation and Phase 4 gate pass.
