# PR-700 — Canonical edit-to-HyperFrames mapping

Status: **VERIFIED (contract level)**

`lib/hyperframes_contracts.py` now maps canonical cuts to stable HyperFrames
fields: IDs, source/asset identity, start/duration, media offsets, speed,
layer/track, transitions, animation, and motion keyframe requirements. Unknown
cut types and unmapped fields fail closed in production mode. The Layer 2
HyperFrames guide documents the same mapping and the production workspace
artifacts.

Evidence:

```text
python -m pytest -q tests/contracts/test_phase7_gate.py tests/tools/test_hyperframes_compose.py
51 passed in 75.25s
```

The live HyperFrames CLI remains separately gated by `PR-703`.
