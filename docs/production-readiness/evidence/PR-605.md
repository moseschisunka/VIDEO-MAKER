# PR-605 — AI image plans through the executor

Status: **VERIFIED**

`ImageSelector` now creates a stable generation plan, enforces immutable
sample approval for batch work before provider invocation, routes through the
provider executor, optionally validates decoded dimensions, and supports a
content-addressed asset cache.

Evidence: selector pre-provider sample-gate test and Phase 6 gate pass.
