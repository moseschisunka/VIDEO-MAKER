# PR-507 — Provider and failure matrix

Status: **VERIFIED**

The Phase 5 matrix exercises a free/local-shaped provider and a cloud-shaped
provider, explicit voice changes, timeout failure, empty/partial output,
provider-independent cache resume, and the approval gate before provider
execution. All failure paths remain retryable without invalid cache entries.

Evidence: `tests/contracts/test_phase5_voice.py::test_provider_failure_matrix_timeout_and_partial_output_are_retryable` and `tests/contracts/test_phase5_gate.py`.
