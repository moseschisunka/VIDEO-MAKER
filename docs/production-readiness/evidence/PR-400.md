# PR-400 — Source and claim-grounding contracts

Status: **VERIFIED**

Research, brief, and script schemas now carry stable profile-independent
grounding metadata: source IDs, canonical locators, access dates, relevant
notes, license/usage constraints, and claim IDs. Script artifacts support an
explicit claim ledger and a required grounding policy while preserving the
legacy `source_ref` spelling for migration. The binding agent policy is in
`skills/meta/claim-grounding.md` and is referenced by the research/script
director skills and reviewer.

Evidence: `tests/contracts/test_phase4_grounding.py` schema and contract tests;
`python -c "...Draft202012Validator.check_schema..."` passes for research,
brief, script, and grounding report schemas.
