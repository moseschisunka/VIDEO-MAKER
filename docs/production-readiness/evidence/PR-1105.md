# PR-1105 — Security, recovery, and rollback drill

- Status: **NOT STARTED**
- Correction date: 2026-09-04

## Repository evidence already available

The repository contains fail-closed alert-rule tests, backup/restore drills,
authentication and path controls, a Prometheus-compatible endpoint, and
documented rollback procedures. The remote-authentication false-override found
during release review was corrected in `eeadb57`; supported workflow
`33830316403` passes release blockers, offline regression, clean install,
container/Remotion, and Phase 10 SLO/load/operations.

## Invalidated operational claims

No retained evidence identifies an actual deployment target, deployed and
previous image digests, timed rollback, post-rollback state comparison,
trusted-edge configuration/results, external paging receipt, or durable
metrics query across a restart. A local fake-sink or written procedure does not
prove those environment-owned controls.

## Required evidence

- `REC-03`: immutable before/after digests, rollback timing, health checks, and
  state-integrity comparison.
- `SEC-06`: deployed origin, bearer, CORS, and rate-limit results including a
  redacted `429` response.
- `OBS-03`: synthetic P0/P1 event and receipt from the approved external sink.
- `OBS-02`: external metrics query proving continuity across service restart.

## Decision

**NOT RUN.** `REL-05`, `REC-03`, `SEC-06`, `OBS-02`, and `OBS-03` remain open.
