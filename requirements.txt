# OpenMontage runtime dependencies are declared once in pyproject.toml.
# This checkout-friendly wrapper preserves the documented ``pip install -r``
# command while ensuring metadata and dependency resolution cannot diverge.
-e .
