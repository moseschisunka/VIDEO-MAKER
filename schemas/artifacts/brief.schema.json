{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "openmontage/artifacts/brief",
  "title": "Video Brief",
  "description": "Creative brief produced by the Idea Explorer agent.",
  "type": "object",
  "required": ["version", "title", "hook", "key_points", "tone", "style", "target_platform", "target_duration_seconds"],
  "properties": {
    "version": { "type": "string", "const": "1.0" },
    "title": { "type": "string", "minLength": 1 },
    "hook": { "type": "string", "description": "Opening hook / attention grabber" },
    "key_points": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1
    },
    "core_message": { "type": "string" },
    "cta": { "type": "string", "description": "Call to action" },
    "tone": { "type": "string" },
    "style": { "type": "string", "description": "Style playbook name or custom description" },
    "target_audience": { "type": "string" },
    "target_platform": {
      "type": "string",
      "enum": ["youtube", "instagram", "tiktok", "linkedin", "generic"]
    },
    "target_duration_seconds": { "type": "number", "minimum": 1 },
    "output_profile": { "type": "string", "minLength": 1, "description": "Canonical media profile selected for the output" },
    "profile": { "type": "string", "minLength": 1, "description": "Compatibility alias for output_profile" },
    "aspect_ratio": { "type": "string", "enum": ["16:9", "9:16", "1:1", "21:9", "4:3"] },
    "reference_material": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Paths or URLs to source material"
    },
    "source_refs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Canonical source IDs or locators used to shape the brief"
    },
    "grounding_contract": {
      "type": "object",
      "description": "Source/claim policy inherited by script and scene stages",
      "properties": {
        "required": { "type": "boolean", "default": true },
        "policy": { "type": "string", "enum": ["block", "revise", "warn"] },
        "source_record_version": { "type": "string", "const": "1.0" }
      },
      "additionalProperties": false
    },
    "angle_options": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "description": { "type": "string" }
        },
        "required": ["name", "description"]
      },
      "description": "2-3 angle options for user to choose from"
    },
    "selected_angle": { "type": "string" },
    "metadata": { "type": "object" }
  },
  "additionalProperties": false
}
